local floating_win = require("plugins.floating_win")
local toggle_term = require("plugins.toggle_term")

local plugins = {
  floating_win = floating_win,
  toggle_term = toggle_term
}

return plugins
