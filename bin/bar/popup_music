#!/usr/bin/env bash

# WIP

# Source Variables
. ~/bin/popup_config

music() {
    music=$(n=$(mpc current); if [ -z "$n" ] ; then echo; else echo " $n"; fi)
	echo "%{F$COLOR_GREEN}${music}"
}

popup_bar $(music)
